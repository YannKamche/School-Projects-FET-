#include<stdio.h>
int main(){
	int num1, num2, count = 0;
	int *top;
	
	printf("Enter a number: ",num1);
	scanf("%d",&num1);
	
	printf("Enter a second number");
	scanf("%d",num2);
	
	int p[num2];
	
	if(*top == num2){
		printf("fdf".p[top]);
		
		else
			for(top <= num2){
				top++;
				p[top] = num1 * count;
				count++;
			}
	return p;
	}
	
	if(top == 0){
		break;
		else
			top--;
			pirntf(*p[top + 1]);
		}
	return p;
}